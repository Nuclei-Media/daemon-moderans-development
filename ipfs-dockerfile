FROM alpine:3.12

RUN apk add --no-cache curl

RUN curl https://dist.ipfs.io/go-ipfs/v0.7.0/go-ipfs_v0.7.0_linux-amd64.tar.gz | tar xz

WORKDIR /go-ipfs

RUN ./install.sh

EXPOSE 4001 5001 8080

CMD ["ipfs", "daemon"]
